import {StyleSheet} from 'react-native';
import {FONT_SIZE, SPACING} from '../../../../../../styles/theme';

const styles = StyleSheet.create({
  dateButtonText: {
    fontSize: FONT_SIZE.text.xs,
  },
  button: {paddingVertical: SPACING.xxs},
});

export default styles;
